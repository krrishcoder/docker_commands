
// this command is used in local machine to build a specialzed image for linux/amd , this OS is used by most of the cloud services.
// so this will push the images on docker hub also
//if you need login do
// docker login
docker buildx build --platform linux/amd64 -t krrishcoder07/myfastapiapp:latest --push .
